// =============================================================
// 资产配置看板 · 全局配置 (示例)
// 请复制此文件为 config.js，以加载私有数据
// =============================================================
window.AFD_CONFIG = {
  // 指定数据目录，默认为 demo_data
  // 如果你需要加载自己的真实数据，请在本地创建 config.js，并将此值改为 "data"
  dataDir: "demo_data",
  sharedDir: "demo_data"
};
